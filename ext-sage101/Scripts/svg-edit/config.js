/*globals svgEditor*/

svgEditor.setConfig({
 extensions: [
	'ext-Sage101.js'
    , 'ext-Sage102.js'

],
	allowedOrigins: [window.location.origin] // May be 'null' (as a string) when used as a file:// URL
});

